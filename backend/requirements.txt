# ClaimCheck backend — production (CPU) dependencies.
#
# IMPORTANT: the CPU-only torch wheel. The default PyPI `torch` wheel for Linux
# bundles the CUDA runtime: ~800MB to download and >2.5GB installed, which
# exceeds the build disk on every free tier. The `+cpu` build is ~200MB and is
# published only on the PyTorch index, so pip is forced to take it from there.
#
# macOS/Windows: `+cpu` wheels are not published. Use `torch==2.5.1` instead
# (already CPU-only on those platforms).
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0
python-multipart==0.0.12

# --- ML / inference ---
torch==2.5.1+cpu
transformers==4.46.3
sentence-transformers==3.3.1   # semantic retrieval; disable via ENABLE_SEMANTIC_RETRIEVAL=false
rank-bm25==0.2.2
numpy==1.26.4

# --- Storage / cache ---
SQLAlchemy==2.0.36
aiosqlite==0.20.0
redis==5.2.0                   # optional: falls back to in-process cache automatically

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1                  # passlib 1.7.4 crashes on bcrypt>=4.1 (reads bcrypt.__about__)

# --- Ops ---
slowapi==0.1.9
prometheus-client==0.21.0
httpx==0.27.2
python-dotenv==1.0.1

accelerate>=0.26.0
