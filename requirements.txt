# Full development profile. For deployment use backend/requirements.txt
# (CPU-only torch, no optional heavyweight extras).
#
# Removed from the previous version because they do not exist on PyPI and made
# `pip install -r requirements.txt` fail outright:
#   acspell==8.15.0, redis-py==5.0.6 (the package is `redis`),
#   python-jose==3.10.0 (latest is 3.3.0), httpcore==0.22.0 (httpx 0.27 needs httpcore 1.x)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0
python-multipart==0.0.12

# ML/AI
torch==2.5.1+cpu
transformers==4.46.3
sentence-transformers==3.3.1
rank-bm25==0.2.2
spacy==3.8.2
numpy==1.26.4

# Caching
redis==5.2.0

# Database
SQLAlchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
alembic==1.14.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
slowapi==0.1.9

# HTTP
httpx==0.27.2

# Monitoring
prometheus-client==0.21.0

# Data processing
pandas==2.2.3

# Utilities
python-dotenv==1.0.1
orjson==3.10.12
tenacity==9.0.0

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
